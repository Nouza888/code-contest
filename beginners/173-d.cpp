#include<bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0; i<(n); ++i)
using ll = long long;
using P = pair<int, int>;

int max(vector<int> a){
    int max=a.at(0)
    rep(i,a.size()){
        if(a.at(i)>=max){
            
        }
    }

}

int main(){
    int n;
    cin >> n;
    vector<int> a(n);
    rep(i,n){
        cin >> a.at(i);
    }

    return 0;
}